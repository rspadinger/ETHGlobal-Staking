{
    "name": "Staking",
    "version": "1.0.0",
    "description": "",
    "scripts": {
        "compile": "npx hardhat compile",
        "postcompile": "mkdir -p ../ui/abi && cp artifacts/contracts/???.sol/???.json ../ui/abi/???.json && cp artifacts/contracts/???.sol/???.json ../ui/abi/???.json",
        "deploy": "hardhat run scripts/deploy.js"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^3.0.0",
        "hardhat": "^2.28.6"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^5.0.2",
        "dotenv": "^16.3.1"
    }
}
